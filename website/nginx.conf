server {
    listen 80 default_server;
    server_name mimir.smartlarkai.com;

    root /var/www/mimir;
    index index.html;

    # 静态资源长缓存（内容变更时 rsync 会更新文件，浏览器靠文件名之外的
    # Last-Modified 校验，够用且简单）
    location /assets/ {
        expires 7d;
        add_header Cache-Control "public";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/html text/css application/javascript image/svg+xml;
}
